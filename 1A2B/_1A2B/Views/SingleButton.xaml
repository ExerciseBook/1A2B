<UserControl
    x:Class="_1A2B.Views.SingleButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="60"
    d:DesignWidth="60">

    <Grid BorderThickness="1.5">

        <Grid.Resources>
            <Style TargetType="Button">
                <Setter Property="Padding" Value="0"/>
                <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                <Setter Property="VerticalAlignment" Value="Stretch"/>
                <Setter Property="HorizontalAlignment" Value="Stretch"/>
                <Setter Property="BorderThickness" Value="0"/>
            </Style>
        </Grid.Resources>

        <Button x:Name="MyButton" Click="onClick">
            <Grid x:Name="MyGrid" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="10">
                <TextBlock x:Name="MyContent" TextAlignment="Center" FontSize="36"/>
            </Grid>
        </Button>
        
    </Grid>
</UserControl>
